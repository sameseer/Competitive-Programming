class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        ans = []
        for i in nums:
            for j in nums:
                pass
            ans.append(nums[i])
        return ans
        